<Window x:Class="PKS.OrderView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PKS"
        mc:Ignorable="d"
            Title="ChooseComponent" Height="740" Width="1000" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True">
    <Window.Background>
        <ImageBrush ImageSource="/LoginBack.png"/>
    </Window.Background>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="OrderInfo" Background="#99818181" IsReadOnly="True" FontSize="20" Text="Карточка заказа:"></TextBox>
        <Button x:Name="Exit" Content="Готово" HorizontalAlignment="Left" Margin="20,11,0,15" Grid.Row="1" Width="215" FontSize="30" FontWeight="Bold" BorderBrush="Black" BorderThickness="2" Background="{x:Null}" Click="Exit_Click"/>
        <Button x:Name="Print" Content="Печать" HorizontalAlignment="Left" Margin="260,11,0,15" Grid.Row="1" Width="215" FontSize="30" FontWeight="Bold" BorderBrush="Black" BorderThickness="2" Background="{x:Null}" Click="Print_Click"/>
    </Grid>
</Window>
